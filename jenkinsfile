pipeline {
    agent {label 'docker_label'}
        docker { image 'node:7-alpine' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'docker images'
            }
        }
    }
}
